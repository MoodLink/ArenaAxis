server:
  port: 8082

spring:
  application:
    name: message-service
  data:
    mongodb:
      uri: mongodb+srv://doannhb:ga7eNC.VDeARB5Q@cluster0.krzv10n.mongodb.net/arena_message_service?retryWrites=true&w=majority
      database: arena_message_service

management:
  endpoints:
    web:
      exposure:
        include: "*"

logging:
  level:
    org.springframework.cloud.gateway: DEBUG
    com.arenaaxis.messageservice: DEBUG
  pattern:
    console: "%clr(%d{yyyy-MM-dd HH:mm:ss}){blue} %clr([%X{traceId:-},%X{spanId:-}]){cyan} %clr(${LOG_LEVEL_PATTERN:-%5p}) %m%n"

service:
  user:
    url: ${USER_SERVICE_URL:http://localhost:8081}