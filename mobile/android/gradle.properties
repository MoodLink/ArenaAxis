# ===============================
# 💙 Flutter Android build config
# ===============================

# 💾 RAM và vùng nhớ cho Gradle (an toàn cho máy RAM >= 8GB)
org.gradle.jvmargs=-Xmx8G -XX:MaxMetaspaceSize=1G -XX:ReservedCodeCacheSize=512m -Dfile.encoding=UTF-8 -XX:+HeapDumpOnOutOfMemoryError

# ⚙️ Dùng AndroidX và Jetifier để tương thích plugin cũ
android.useAndroidX=true
android.enableJetifier=true

# ⚡️ Tắt incremental cache Kotlin (sửa lỗi "Could not close incremental caches")
kotlin.incremental=false

# 🔄 Cho phép build song song (nhiều module cùng lúc)
org.gradle.parallel=true

# 💨 Bật Gradle build cache để tăng tốc build lại
org.gradle.caching=true

# ☕️ Dùng JDK 11 (Java 8 đã obsolete)
kotlin.jvm.target=11
